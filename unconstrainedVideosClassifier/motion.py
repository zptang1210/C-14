class motion:
    def __init__(self, blockNum, hash, innerRadius, outerRadius, motionAngle, clockwiseDirect):
        self.blockNum = blockNum
        self.hash = hash
        self.innerRadius = innerRadius
        self.outerRadius = outerRadius
        self.motionAngle = motionAngle
        self.clockwiseDirect = clockwiseDirect